# database-reference — 참조 DBMS 소스와의 설계 비교

`database-reference`는 팀원이 예전에 작성한 다른 DBMS의 소스 트리(헤더 중심, 실행기 `.cc` 부재)다. **문서·소스·발표자료 어디에도
제품명을 쓰지 않고 "database-reference 참고"로만 표기한다**(사용자 지시 2026-09-16). 소스 트리의 로컬 위치는 컨테이너 사실이라
`members/<이름>/README.md`에만 적는다.

| 문서 | 무엇 | 기준 |
|---|---|---|
| [expc-식컴파일러-비교.md](expc-식컴파일러-비교.md) | 레지스터 머신형 식 컴파일러(`expc`) 대 CUBRID `expr_compile` — 대응표, 가져온 것(D27 합성 호이스팅·D28 n항 체인), 가져오지 않은 것, 숫자형·값 표현(§6), 실행 시점 헤더 비교(§7, §7의 복사 판정은 아래 문서로 정정) | PR#7658 `2628b943d`~`9a13c297d`, 2026-09-16 |
| [실행계층-데이터계층-분리-분석.md](실행계층-데이터계층-분리-분석.md) | 실행 계층 중심 설계분석서 — 계층 절단 위치(dbsvr/ssvr), 노드·행 표현·복사 회계(CUBRID는 PEEK 기본), 메모리·임시공간, 데이터 계층 경계(`Adaptor`/`Scanner`/CR 블록 vs `heap_*`/pgbuf), 우월·뒤처짐 판정, **데이터 계층 분리 3겹 제안**(계약 → 블록 통화 → 프로세스) | develop `a7a1db84b` + PR#7658, 2026-09-16 |

읽는 규약: 비교 결과 중 **CUBRID 코드 사실**은 `modules/src-*.md` §2에 (출처·리비전 유지), 설계 결정은 `claude-workspace/projects/CBRD-*/`에.
이 디렉터리는 "저쪽은 어떻게 했고 우리와 무엇이 다른가"만 담는다.
