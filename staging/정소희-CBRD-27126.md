# 정소희 — CBRD-27126 (옵티마이저 비용 단가 파라미터화, PR#7993) 환류 대기

작성: 정소희 (.50) · 기준 리비전 upstream/develop `4f0f90b62` (+ 브랜치 `87b25579e`) · 2026-09-21~22

- **[src-optimizer §2] `ISCAN_IO_HIT_RATIO` 는 죽은 상수다** (.50, 4f0f90b62, 2026-09-21): CBRD-27094(`bacd5d0d2`)가 `qo_nljoin_cost` 의 평탄 캐시 할인을 Mackert-Lohman 포화로 바꾼 뒤 살아 있는 참조가 없다 — query_planner.c:131 정의, 3845 주석, 3931-3932 `#if 0` 블록만. 파라미터로 노출하면 효과 없는 손잡이. **미수정**(정리 별건).
- **[src-optimizer §2] `QO_EFFECTIVE_CACHE_PAGES 32768.0` 이 Mackert-Lohman 의 캐시 가정이고 서버 `data_buffer_pages` 를 클라이언트가 읽지 못해 하드코딩됐다** (.50, 4f0f90b62, 2026-09-21): query_planner.c:132 주석이 자백. PR#7993 이 `cost_effective_cache_pages`(INT, 페이지 수)로 노출. `qo_mackert_lohman_pages` 는 b=0 이면 T>b 분기에서 lim=0 → pages_fetched=N 으로 안전 퇴화(0 나눗셈 없음).
- **[src-optimizer §2] 27094 이후 `qo_nljoin_cost` 의 `MIN (card*heap_io, heap_fetched)` 는 페이지 수끼리 비교한다 — IO 단가를 곱하려면 `iscan_heap_io`·`variable_io_cost` 와 `heap_fetched`·`leaf_fetched` 양변에 같은 인수를 곱해야 포화 지점이 불변** (.50, 87b25579e, 2026-09-21): 한쪽만 곱하면 단가가 비용을 스케일하는 대신 포화 지점을 옮긴다. 실측: 비커버링 NL 1만 프로브에서 random×2 → +359(포화 357 페이지 ×2), cache=0 → 18,326→1,017,969.
- **[src-optimizer §2] `TEMP_SETUP_COST` 는 `fixed_cpu_cost` 에 더해지는 정책 오프셋이다(페이지 아님)** (.50, 4f0f90b62, 2026-09-21): query_planner.c:3109. 구 PR#7561 이 여기 seq 페이지 단가를 곱한 것은 단위 오류.
- **[src-optimizer §2] 비용 모델의 값은 `QO_CPU_WEIGHT(0.0025)` 의 정수배라 `.5` 경계가 흔하다 — `(double) 0.0025f`(−2.2e-8) 만으로도 덤프의 정수 반올림이 뒤집힌다(같은 볼륨에서 NL 58→57 실측)** (.50, 06b647fc9, 2026-09-21): 그래서 PR#7993 은 CPU 단가를 정수 역수 `cost_cpu_tuples_per_page=400`(1.0/400 == 0.0025 비트 동일)으로 노출. 시스템 파라미터에 double 타입은 없다.
- **[src-optimizer §2] 커버링 인덱스 프로브(`count(*)`)는 힙 페치 0·프로브당 리프 IO ~0.0002 페이지라 NL 포화 상한이 걸리지 않는다** (.50, 87b25579e, 2026-09-21): `cost_effective_cache_pages` 효과를 보려면 비커버링(`count(b.pad)`) 프로브가 필요.
- **[src-base §2] `prm_get_*_value()` 는 클라이언트(비 SERVER_MODE)에서 `ALWAYS_INLINE` 배열 인덱스 1회이고, develop 비용 함수 3곳이 이미 그렇게 읽는다(`qo_sort_cost` SR_NBUFFERS, `qo_follow_cost` PB_NBUFFERS, `qo_hjoin_cost` MAX_HASH_LIST_SCAN_SIZE)** (.50, 4f0f90b62, 2026-09-21): system_parameter.h:892. 비용 함수용 캐시 계층은 근거 없는 전역 상태.
- **[src-base §2] 플랜에 영향 주는 클라이언트 파라미터는 `PRM_FOR_QRY_STRING` 이 있어야 XASL 캐시 키에 들어간다** (.50, 87b25579e, 2026-09-21): `sysprm_print_parameters_for_qry_string()`(system_parameter.c:12187) 이 `PRM_IS_DIFFERENT && PRM_IS_FOR_QRY_STRING`(CS; SA 는 FOR_QRY_STRING 전부) 인 것만 출력 → `parse_tree_cl.c:3100` 이 `?` 뒤에 붙임 → `execute_statement.c` SHA1. `PRM_DIFFERENT` 는 모든 `prm_set_*_value()` 끝의 `sysprm_update_flag_different()` 로 갱신(CS_MODE + FOR_QRY_STRING 전용). 루프 상한 `MAX_SYSTEM_PARAMS = prm_Def_size`(동적). 전례 `max_hash_list_scan_size`. 없으면 단가를 바꾼 세션이 옛 플랜을 재사용(재현 `projects/CBRD-27126/repro/cache_repro.sql`).
- **[src-base §2] `prm_Def[]` 는 끝에만 추가한다 — PL 서버(SysParam.java)가 ordinal 을 하드코딩** (.50, 4f0f90b62, 2026-09-21): system_parameter.c 배열 뒤 주석 + `static_assert (PRM_ID_STORED_PROCEDURE_DUMP_ICODE == 354)`.
- **[src-query §2] XASL 캐시 키 계산 지점은 4곳이고 `PT_PRINT_DIFFERENT_SYSTEM_PARAMETERS` 는 `do_prepare_select` 에만 있다** (.50, 87b25579e, 2026-09-22): execute_statement.c 15268(select)·9928(update)·11310(delete)·11971(insert_internal). 따라서 `PRM_FOR_QRY_STRING` 파라미터(7993 의 `cost_*`, 기존 `max_hash_list_scan_size` 등)는 **UPDATE/DELETE/INSERT 서버측 플랜의 캐시 키에 반영되지 않는다**. **미수정**(결함 후보 — 발번 여부 사용자 결정).
- **[src-parser §2] `CUSTOM_PRINT_4_SHA_COMPUTE` = `PT_CONVERT_RANGE|PT_PRINT_QUOTES|PT_PRINT_USER|PT_PRINT_HOST_VAR_COUNT|PT_PRINT_DBLINK_INFO`(execute_statement.c:108)이고 `PT_PRINT_HOST_VAR_COUNT` 는 캐시 키 출력에서만 쓰여 `parser_print_tree` 가 `;bind_var_cnt=N` 을 붙인다** (.50, 87b25579e, 2026-09-22): parse_tree_cl.c:3115-3137. `custom_print` 는 32비트 전부 사용 중(`PT_PRINT_LOWER=1<<30`, `0x80000000`=SUPPRESS_DBLINK_PUSHED) — 새 출력 플래그 비트가 없다.
- **[src-parser §2] `print_type_ambiguity` 는 호스트 변수 타입 모호성이 아니라 `pt_print_value` 의 MONETARY(통화 NULL)·미지 값 타입에서 세워진다** (.50, 87b25579e, 2026-09-22): parse_tree_cl.c:17345, 17387. 세워지면 `cannot_prepare=1`(캐시 포기).
- **[src-compat §2] `parser->flag.set_host_var` 는 `do_cast_host_variables_to_expected_domain()` 이 값을 기대 도메인으로 캐스팅한 뒤에만 1 이 된다(db_vdb.c:3248) — PREPARE·compile-first(`do_recompile_and_execute_prepared_statement` SELECT 경로는 `is_subsession_for_prepared=false` 로 캐스트를 피함)는 0, peek 재플랜·DML bind-first 는 1** (.50, 87b25579e, 2026-09-22): 값 바인드 컴파일과 값 없는 컴파일을 이 플래그로 구분할 수 있다. 캐스팅이 타입을 기대 도메인으로 정규화하므로(UNKNOWN/ENUM 도메인·CHAR 도메인의 VARCHAR 값·NULL 만 예외) 키 시점의 바인드 타입은 대부분 기대 도메인과 같다.
- **[src-parser §2] 타입 검사기는 `set_host_var==0` 이면 호스트 변수를 무타입으로 보고 원하는 타입으로 강제하고(`pt_coerce_value_internal` type_checking.c:19657), `==1` 이면 PT_VALUE 로 fallthrough 해 값의 타입으로 판단한다** (.50, 87b25579e, 2026-09-22): 값 바인드 컴파일 플랜은 타입 의존이다.
- **[src-compat §2] peek 재플랜(`do_replan_statement_with_bind_peek`, db_vdb.c:3497)은 `xasl_id` 해제 + `recompile=1` 로 `do_prepare_statement` 를 다시 돌려 키를 재계산하고 같은 sha1 엔트리를 교체한다** (.50, 87b25579e, 2026-09-22): 그래서 첫 커넥션의 값-고정 플랜을 다른 커넥션이 물려받는다(7476 Remarks 의 OFF 의미). 바인드 지문 `histogram_bind_fingerprint`(histogram_cl.cpp:2877) 는 EQ `sel×1e12`·범위 `sel×100` 양자화 성분을 접으며 재플랜 판정에만 쓰이고 캐시 키에는 없다(7476 후속 미발번).
- **[하네스] `implement_pack --symbols` 에 `system_parameter.c` 함수를 넣으면 같은 디렉터리 그래프로 `copylogdb`·`pl_session`·`heap_file` 까지 끌려 24개를 넘는다 — `prm_Def[]`·enum 은 함수가 아니라 팩이 담지 못하므로 plan 에 데이터 편집으로 명시하고 게이트(파일 단위 범위 검사)에 맡긴다** (.50, harness e9718fe, 2026-09-21). **미수정**(도구 한계 기록).
- **[하네스] `implement_gate` 의 `-fsyntax-only` 는 `~/dev/build/build_x86_64_release/compile_commands.json` 을 요구하는데 goto 빌드 트리에 없고 `ninja -t compdb` 도 최상위 build.ninja 규칙이 없어 0 엔트리다 — 문법 검사는 goto 빌드로 대체** (.50, 2026-09-21). **미수정**.
- **[하네스] `harness-review` 판정기는 finding 앵커가 diff ±3줄 밖이면 inconclusive 로 낸다 — 매크로 정의(파일 상단) 변경을 지적할 때는 그 매크로를 쓰는 변경 줄에 앵커한다** (.50, 2026-09-21).
