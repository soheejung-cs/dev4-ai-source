# 정소희 — CBRD-27043 (db_histogram 뷰) 환류 대기

작성: 정소희 (.50) · 기준 리비전 upstream/develop `6497720ec` · 2026-09-17

- **[src-object §2] `_db_histogram.class_of` 는 `_db_class` 행이 아니라 클래스 MOP 자체다** (.50, 6497720ec, 2026-09-17): `smt_add_histogram`(schema_template.c:2094)·`db_get_histogram`(histogram_cl.cpp:2214)·`get_histogram_for_write` 가 `db_make_object (&value, classop)` 으로 키를 만든다. 그래서 뷰에서 이름을 얻으려면 `db_index` 식 경로식(`[i].[class_of].[class_name]`)이 아니라 `_db_class [c]` 조인 `[h].[class_of] = [c].[class_of]` 이 필요하고, `_db_auth.object_of` 와는 바로 비교된다(db_attribute 스펙의 `{[c].[class_of]}` 자리에 `[h].[class_of]`). PR#7976 에서 수정.
- **[src-object §2] 시스템 뷰의 컬럼 도메인(`get_view_*` 의 columns)이 query spec 의 결과 타입을 덮어쓴다** (.50, 6497720ec, 2026-09-17): `db_histogram` 스펙이 `CAST (… AS NUMERIC(18, 12))` 를 해도 도메인이 `double` 이면 `0.000000000000000e+00` 로 나오고, `{"class_name", "object"}` 면 문자열 비교가 `'=' operator is not defined on types object and char` 가 된다(재현 `projects/CBRD-27043/repro/repro_27043.out`). 스펙과 도메인은 1:1 로 맞춰야 한다. PR#7976 에서 수정.
- **[src-object §2] `schema_system_catalog_install_query_spec.cpp` 의 권한 필터는 인라인 텍스트, `AUTH_CHECK_*`·`CURRENT_USER_GROUPS_SUBQUERY` 매크로는 `schema_information_schema_install_query_spec.cpp` 에만 정의돼 있다** (.50, 6497720ec, 2026-09-17): AGENTS.md "Information Schema Rules 9. Use auth macros" 는 information_schema 파일 기준이고 시스템 카탈로그 뷰 파일에서는 db_index·db_partition·db_attribute 전부 인라인 서브쿼리 3개(DBA 그룹 / 소유자 / SELECT 피부여자)를 반복한다. **미수정**(문서 불일치).
- **[src-object §3] 시스템 뷰 정의 변경은 기존 DB 에 반영되지 않는다 — 카탈로그 마이그레이션 없음** (.50, 2026-09-17): 뷰 정의는 createdb 의 `catcls_init`/`install_system_metadata` 경로에서 `_db_class` 에 저장된다. #7791(CBRD-27258 뷰 스펙 개편)도 같은 조건. 기존 develop DB(tpch-db·joinorder)는 옛 `db_histogram` 정의를 유지. **미수정**(설계상 한계).
- **[하네스] 리뷰 하네스 perf_claims 감지기가 PR 본문 표의 행 수("3행/1행")·버퍼 크기("2048 → 4096")를 성능 수치로 읽어 MEAS-01/05 를 오탐한다** (.50, harness 0a56ed5, 2026-09-17): PR#7976 자기 리뷰. 성능 주장 없는 카탈로그 뷰 PR 에서 🟡 2건. → dev4-review-workspace 이슈 후보. **미수정**.
- **[src-object §2] `CNT_CATCLS_OBJECTS`(schema_system_catalog_constants.h) 는 시스템 클래스의 일반 object 도메인 개수이고, TRUNCATE 의 참조 판정 기준선이다 — 시스템 뷰 컬럼의 *타입*을 바꾸면 이 값이 움직인다** (.50, PR#7976 `15279602e`, 2026-09-18): `schema_class_truncator.cpp` 는 `SELECT COUNT(*) FROM _db_domain WHERE data_type=5 AND (class_of.unique_name='<대상>' OR class_of IS NULL) AND ROWNUM <= CNT_CATCLS_OBJECTS + 1` 로 세어 **기준선을 넘으면** "사용자 클래스가 참조한다" 로 보고 행 삭제(heap 보존), 아니면 heap 파괴 경로를 탄다. `db_histogram.class_name` 을 OBJECT→VARCHAR 로 바꾸니 실제 개수가 9→8 이 됐고 상수를 안 내리자 참조가 있는 `DONT_REUSE_OID` 테이블도 `8+1=9` 로 기준선을 못 넘어 **heap 이 파괴·재생성돼 OID 재사용 방지가 풀렸다**. PR#7976 에서 8 로 수정. 헤더·truncator 의 CAUTION 주석이 상수 9 인데 본문은 6 이라고 적고 있던 것도 함께 정정.
- **[src-object §2] 이 결합을 감시하는 TC 가 세 층으로 있다** (.50, 2026-09-18): ① `shell cbrd_23954/catcls_general_object` 가 개수 자체를 세고(주석에 "이 값이 바뀌면 `truncate_heap()` 을 같이 고쳐라" 명시, CBRD-23983) ② `shell cbrd_23954/use_delete` 가 8종 참조 타입 각각에 대해 TRUNCATE 전후 heap `vfid` 동일성을 보고 ③ `sql bug_3339`(CUBRIDSUS-3339) 가 TRUNCATE 후 dangling OID 역참조가 NULL 인지 본다. 상수 누락 시 ①은 출력차, ②③은 **동작 차이**로 나타난다 — ②③ 을 답안 재블레싱하면 회귀를 통과시키므로 정렬 대상이 아니다. `shell cbrd_20149_xasl` 의 truncate 답안 5건은 플랜 덤프가 truncator 질의문을 그대로 실어 `ROWNUM <= CNT+1` 이 노출되므로 상수 변경 시 함께 정렬해야 한다.
- **[하네스] gha-ci 아티팩트 서버 레이아웃이 바뀌었다 (2026-09 중순 이후)** (.50, run 35214042694, 2026-09-18): 구 `runs/<id>/failed.list`·`results/<샤드>/…` 가 아니라 **`runs/<id>/{build,tc,shell,sql,medium,plan,stamps}/`** 구조다. 실패 목록은 `<스위트>/collect/failed.list`(`<샤드>\t<케이스>`)와 `<스위트>/collect/verdict`, 샤드별 산출물은 `<스위트>/shard/<NN>/{build.read,tc.read,test-results/test-shell.xml,artifacts/{feedback.log,ctp_log.tgz},test_status.data}`. `tc-analysis` 스킬 §2 의 경로는 이 구조로 갱신 필요. **미수정**(스킬 문서).
- **[하네스] 셸 케이스를 직접 돌릴 때 완료 판정은 프로세스로 한다** (.50, 2026-09-18): `cbrd_20149_xasl` 은 `.sql` 39개마다 `cubrid server restart` + `plandump` 를 돌려 20분 이상 걸리고 `<case>.result` 를 **진행 중에 한 줄씩** 쓴다. result 파일 존재·내용으로 완료를 판정하면 중간 상태(17/39)를 완료로 오독한다 — `pgrep -f <case>.sh` 로 본다.
- **[하네스] `pre_clear_check.sh` §7 이 TC 리포의 브랜치를 `origin`(내 포크) 기준으로 판정해 살아 있는 `tc/pr-<n>` 을 삭제 후보로 찍는다** (.50, 2026-09-18): TC PR 의 head 는 **`upstream`(CUBRID 조직)** 의 `tc/pr-<n>` 이고 내 포크에는 없다. 그래서 진행 중인 PR#3536·#4196 의 브랜치가 "origin 에서 삭제됨 = TC PR 정리 완료" 로 나왔다. 로컬 브랜치를 지워도 원격은 무사하지만 작업 중인 브랜치를 잃는다. 판정은 `gh pr list --repo CUBRID/<tc리포> --head tc/pr-<n> --state open` 또는 `git ls-remote upstream tc/pr-<n>` 으로 해야 한다. **미수정**(스킬 도구).
