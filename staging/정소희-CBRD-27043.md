# 정소희 — CBRD-27043 (db_histogram 뷰) 환류 대기

작성: 정소희 (.50) · 기준 리비전 upstream/develop `6497720ec` · 2026-09-17

- **[src-object §2] `_db_histogram.class_of` 는 `_db_class` 행이 아니라 클래스 MOP 자체다** (.50, 6497720ec, 2026-09-17): `smt_add_histogram`(schema_template.c:2094)·`db_get_histogram`(histogram_cl.cpp:2214)·`get_histogram_for_write` 가 `db_make_object (&value, classop)` 으로 키를 만든다. 그래서 뷰에서 이름을 얻으려면 `db_index` 식 경로식(`[i].[class_of].[class_name]`)이 아니라 `_db_class [c]` 조인 `[h].[class_of] = [c].[class_of]` 이 필요하고, `_db_auth.object_of` 와는 바로 비교된다(db_attribute 스펙의 `{[c].[class_of]}` 자리에 `[h].[class_of]`). PR#7976 에서 수정.
- **[src-object §2] 시스템 뷰의 컬럼 도메인(`get_view_*` 의 columns)이 query spec 의 결과 타입을 덮어쓴다** (.50, 6497720ec, 2026-09-17): `db_histogram` 스펙이 `CAST (… AS NUMERIC(18, 12))` 를 해도 도메인이 `double` 이면 `0.000000000000000e+00` 로 나오고, `{"class_name", "object"}` 면 문자열 비교가 `'=' operator is not defined on types object and char` 가 된다(재현 `projects/CBRD-27043/repro/repro_27043.out`). 스펙과 도메인은 1:1 로 맞춰야 한다. PR#7976 에서 수정.
- **[src-object §2] `schema_system_catalog_install_query_spec.cpp` 의 권한 필터는 인라인 텍스트, `AUTH_CHECK_*`·`CURRENT_USER_GROUPS_SUBQUERY` 매크로는 `schema_information_schema_install_query_spec.cpp` 에만 정의돼 있다** (.50, 6497720ec, 2026-09-17): AGENTS.md "Information Schema Rules 9. Use auth macros" 는 information_schema 파일 기준이고 시스템 카탈로그 뷰 파일에서는 db_index·db_partition·db_attribute 전부 인라인 서브쿼리 3개(DBA 그룹 / 소유자 / SELECT 피부여자)를 반복한다. **미수정**(문서 불일치).
- **[src-object §3] 시스템 뷰 정의 변경은 기존 DB 에 반영되지 않는다 — 카탈로그 마이그레이션 없음** (.50, 2026-09-17): 뷰 정의는 createdb 의 `catcls_init`/`install_system_metadata` 경로에서 `_db_class` 에 저장된다. #7791(CBRD-27258 뷰 스펙 개편)도 같은 조건. 기존 develop DB(tpch-db·joinorder)는 옛 `db_histogram` 정의를 유지. **미수정**(설계상 한계).
- **[하네스] 리뷰 하네스 perf_claims 감지기가 PR 본문 표의 행 수("3행/1행")·버퍼 크기("2048 → 4096")를 성능 수치로 읽어 MEAS-01/05 를 오탐한다** (.50, harness 0a56ed5, 2026-09-17): PR#7976 자기 리뷰. 성능 주장 없는 카탈로그 뷰 PR 에서 🟡 2건. → dev4-review-workspace 이슈 후보. **미수정**.
