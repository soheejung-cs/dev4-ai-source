# 정소희 — CBRD-27171 (PR#7613 머지 후 리뷰·재측정) 에서 확인한 소스 사실

작성: 정소희 (.50) · 기준 리비전: develop `b319ce1ab`(2026-09-11) / PR#7613 head `3c2d05b2a` · 2026-09-17
상태: **환류 대기** — 모듈 §2·§3 로 옮기면 이 파일을 지운다.

- **[src-base §2 / src-query §2] trace 의 모든 `time` 값과 csql `;time on` 은 `tsc_getticks()` 하나를 쓴다** (.50, `b319ce1ab`, 2026-09-17 static+동적):
  `scan_manager.c` `elapsed_scan`/`elapsed_lookup`/`elapsed_hash_build`, `query_executor.c` `xasl_stats.elapsed_time`·groupby/orderby, `csql.c:2296~2394` 모두 `tsc_getticks`+`tsc_elapsed_time_usec`.
  현대 리눅스에는 `/sys/devices/system/cpu/sched_mc_power_savings` 가 없어 `power_Savings=1` → `clock_gettime` 경로. develop `164223528`(#7918) 이전은 `CLOCK_REALTIME_COARSE`(HZ 에 따라 1~4ms 양자화), 이후 `CLOCK_MONOTONIC`.
  **그 경계를 걸친 두 빌드의 trace 시간·csql 시간은 비교 불가** — 구 빌드 csql 시간은 `0.089000 sec` 처럼 ms 단위로만 찍힌다(실측). **미수정(의도된 변경)**
- **[src-query §2] index scan 의 `lookup time` 은 힙 lookup + 데이터 필터 평가를 행마다 따로 재서 더한 값** (.50, `b319ce1ab`): `scan_manager.c:6709~6718` 이 `scan_next_index_lookup_heap (…, &data_filter, …)` 을 `thread_is_on_trace` 일 때 행마다 `tsc_getticks` 두 번으로 감싼다.
  파생 데이터 필터(27171)가 늘면 이 구간이 늘고, trace 자체가 행당 시계 호출 2회를 추가한다. **미수정**
- **[src-optimizer §2] WHERE 레벨 SARG 가 LEFT/FULL OUTER 의 null-padded 노드에 붙으면 `QO_TC_AFTER_JOIN` 으로 재분류된다** (.50, `b319ce1ab` `query_graph.c` 6495~6515): `QO_TERM_CLASS==QO_TC_SARG` 이고 ON 조건 항이 아닌데 `QO_NODE_PT_JOIN_TYPE` 이 LEFT_OUTER/FULL_OUTER 면 after-join. 27171 의 파생 항(location 0, 단일 노드)도 같은 경로 → `IS NULL` 분기 형태에서도 결과 보존. **미수정**
- **[src-optimizer §2] 파생 항(OR-derived·LIKE-derived) 선택도 회계는 `qo_derived_term_compensate()`(query_graph.c 8886, #7787) 한 곳이다** (.50, `b319ce1ab`): `qo_add_term` 루프 직후 1회. origin 은 `qo_derived_term_origin()` 이 구조로 찾는다(spec≥2 · 파생 항 노드/세그먼트를 포함 · 비파생 disjunction). origin 당 나눗셈 1회 `sel(origin)/Π sel(derived)`(1.0 clamp), 포함관계 위반이면 파생 쪽을 올림, origin 없으면 파생 항 sel=1 + 인덱스 불가(무해화). `qo_node_add_sarg()` 는 모든 sarg 를 계수, `planner_visit_node()` 는 파생 항 skip 유지. 유지/드랍 상한 `0.5` 는 `qo_analyze_term()` 2835 와 `make_pred_from_plan()` 1754 두 곳 리터럴. **미수정**
- **[src-optimizer §3] 27171 단독(파생 항 미계수, `c283e799d`)은 통계가 있어도 part 구동 플랜을 고르지 못한다** (.50, 2026-09-17 동적, 중형 200k/2M): A(344782b04)와 같은 lineitem 구동 NL(readkeys 143,158, 238 ms). #7787 포함 빌드에서 part 구동 NL(readkeys 465, 39.7 ms). 최종 빌드의 Q19 이득은 27171+27306 합작. **머지된 상태(수정됨)**
